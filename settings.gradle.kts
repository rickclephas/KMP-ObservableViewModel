pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}

rootProject.name = "kmp-observableviewmodel"

include(":kmp-observableviewmodel-core")
